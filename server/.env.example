# Copy this file to .env and fill in your real values.
# .env is already in .gitignore — never commit your real secrets.

# --- PostgreSQL (local) ---
DB_USER=postgres
DB_PASSWORD=2631
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tecsc

# --- Gmail (for sending the password-reset code) ---
GMAIL_USER=tecsc76@gmail.com
GMAIL_APP_PASSWORD=htvh bnba meht wsii

# --- The one lifetime admin account (used only when running seed-admin) ---
ADMIN_USERNAME=TECSC
ADMIN_PASSWORD=tecsc@768257
ADMIN_EMAIL=tecsc76@gmail.com

# --- Server ---
PORT=4000